services:
#  api:
#    build:
#      context: ./ExamHub.API
#      dockerfile: Dockerfile
#    container_name: api
#    restart: unless-stopped
#    ports:
#      - "5000:8080"
#    environment:
#      ConnectionStrings__Default: "Host=postgres;Database=content;Username=admin;Password=content@123"
#      ConnectionStrings__MongoDB: "mongodb://admin:admin@123@mongo:27017/"
#      Redis__ConnectionString: "redis:6379,password=redis@123"
#      MinIO__Endpoint: "minio:9000"
#      MinIO__AccessKey: "admin"
#      MinIO__SecretKey: "admin@123"
#      RabbitMQ__Host: "rabbitmq"
#      RabbitMQ__Username: "admin"
#      RabbitMQ__Password: "rabbit@123"
#      Jwt__SecretKey: "your-super-secret-256-bit-key"
#      Jwt__Issuer: "ExamHub"
#      Jwt__Audience: "ExamHubUsers"
#    depends_on:
#      postgres:
#        condition: service_healthy
#      redis:
#        condition: service_started
#      mongo:
#        condition: service_started
#      minio:
#        condition: service_started
#      rabbitmq:
#        condition: service_started
#
#  frontend:
#    build:
#      context: ./frontend
#      dockerfile: Dockerfile
#    container_name: frontend
#    restart: unless-stopped
#    ports:
#      - "3000:80"
#    depends_on:
#      - api
#
#  nginx:
#    image: nginx:alpine
#    container_name: nginx
#    restart: unless-stopped
#    ports:
#      - "80:80"
#    volumes:
#      - ./nginx.conf:/etc/nginx/nginx.conf:ro
#    depends_on:
#      - api
#      - frontend

  rabbitmq:
    image: rabbitmq:4.1.0-management
    container_name: rabbitmq
    restart: unless-stopped
    ports:
      - '5672:5672'
      - '15672:15672'
    environment:
      RABBITMQ_DEFAULT_USER: admin
      RABBITMQ_DEFAULT_PASS: rabbit@123

  minio:
    image: minio/minio:RELEASE.2025-03-12T18-04-18Z-cpuv1
    container_name: minio
    restart: unless-stopped
    ports:
      - '9000:9000'
      - '9001:9001'
#    volumes:
#      - ./storage:/data
    environment:
      MINIO_ROOT_USER: admin
      MINIO_ROOT_PASSWORD: admin@123
    command: server --console-address ":9001" /data

  postgres:
    image: postgres:17-alpine
    container_name: postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: examhub@123
      POSTGRES_DB: examhub
    volumes:
      - ./database_schema.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: "pg_isready -U admin --dbname=examhub"
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: 'bitnami/redis:latest'
    container_name: redis
    restart: unless-stopped
    environment:
      REDIS_PASSWORD: redis@123
      REDIS_MASTER_PASSWORD: redis@123
    ports:
      - '6379:6379'

  mongo:
    image: 'mongo:latest'
    container_name: mongo
    restart: unless-stopped
    ports:
      - '27017:27017'
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: admin@123

volumes:
  pgdata:
